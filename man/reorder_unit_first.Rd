% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_preprocess.R
\name{reorder_unit_first}
\alias{reorder_unit_first}
\title{Reorder Rows to Prioritize a Specific Unit}
\usage{
reorder_unit_first(data, unit_pattern)
}
\arguments{
\item{data}{A data frame that contains a \code{unit} column.}

\item{unit_pattern}{A string pattern used to identify rows to bring to the top (e.g., "treated").}
}
\value{
A data frame with reordered rows, where rows matching the pattern come first.
}
\description{
This function reorders a data frame so that rows matching a specific pattern in the \code{unit} column appear first.
This is used for placing a treated target unit at the top of the data frame
before applying methods like synthetic control or matching.
}
\examples{
df <- data.frame(unit = c("control1", "treated", "control2"), value = c(1, 5, 2))
reorder_unit_first(df, "treated")

}
