% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_analysis.R
\name{sensitivity_param}
\alias{sensitivity_param}
\title{Sensitivity Analysis for Algorithm Hyperparameters (Constraint Slack)}
\usage{
sensitivity_param(
  eta_values,
  B,
  F,
  Y,
  X,
  Z,
  t_max,
  dr,
  dt,
  i_max,
  NSE_Z_baseline,
  NSE_X_baseline
)
}
\arguments{
\item{eta_values}{A numeric vector of candidate values for both \code{eta_X} and \code{eta_Z}.
The function evaluates all combinations via a grid search.}

\item{B}{A list of combinations of a budgeting vector.}

\item{F}{A numeric matrix of outcomes in the reference domain. The first row is the treated unit.}

\item{Y}{A numeric matrix of outcomes in the target domain.}

\item{X}{A numeric matrix of covariates in the target domain.}

\item{Z}{A numeric matrix of covariates in the reference domain.}

\item{t_max}{Integer. Number of time periods in reference domain.}

\item{dr}{Integer. Number of covariates in \code{Z}.}

\item{dt}{Integer. Number of covariates in \code{X}.}

\item{i_max}{Integer. Total number of units (treated + control).}

\item{NSE_Z_baseline}{Numeric. Baseline normalized squared error for \code{Z}.}

\item{NSE_X_baseline}{Numeric. Baseline normalized squared error for \code{X}.}
}
\value{
A data frame with one row per \code{(eta_X, eta_Z)} combination, including:
\describe{
\item{eta_X}{Hyperparameter controlling constraint slack in the target domain (\code{X}).}
\item{eta_Z}{Hyperparameter controlling constraint slack in the reference domain (\code{Z}).}
\item{diff_outcome_ref}{Absolute difference between observed and synthetic means in the reference domain.}
\item{diff_outcome_tar}{Absolute difference between observed and synthetic means in the target domain.}
\item{ett_synthetic}{Estimated treatment effect on the treated (ETT) from the synthetic control.}
}
}
\description{
This function performs a sensitivity analysis over the algorithm's hyperparameters \code{eta_X} and \code{eta_Z},
which control the tolerance for the constraints in the optimization procedure. Specifically, these
parameters bind the error for covariates in the target domain (\code{X}) and
the reference domain (\code{Z}).
}
\details{
The hyperparameters \code{eta_X} and \code{eta_Z} are introduced as algorithmic constants that define
the allowable slack in the covariate reconstruction constraints. By varying these parameters,
the function quantifies how sensitive the synthetic control results are to the tightness of the
optimization constraints.
}
\examples{
\dontrun{
eta_vals <- c(0.01, 0.05, 0.1, 0.2)
result <- sensitivity_param(
  eta_values = eta_vals,
  F = F_matrix,
  Y = Y_matrix,
  X = X_matrix,
  Z = Z_matrix,
  t_max = 10,
  dr = 4,
  dt = 5,
  i_max = 20,
  NSE_Z_baseline = 0.05,
  NSE_X_baseline = 0.07
)
}

}
\seealso{
\code{\link{find_best_B}}, \code{\link{sc_ett}}
}
